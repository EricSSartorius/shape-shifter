// Fixed dimensions for the phone
export const SCREEN_HEIGHT = 640;
export const SCREEN_WIDTH = 360;

// key press codes
export const BACKSPACE = "Backspace";

// Used for fetching and storing data in local storage
export const LAYOUT_ID = "LAYOUT_ID";

// Key used for fetching via useSWR
export const LAYOUTS = "layouts";

// Starting color for elements
export const DEFAULT_ELEMENT_COLOR = "#A943C9";
